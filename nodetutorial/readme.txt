the server call upon index.html, 

it reach out to local host at port 8080, and loaded the socket file

it create a new variable socket, grabbed that socket connection

it(client) has a form, onclicked, it emits 'testconnection' and sends username

the server has a test connection which emits 'test this' and sends the return data back to client, other clients, or everybody

the index (client) has a socket 'test this' which alerts the data

reference: https://www.youtube.com/watch?v=EQ_kenymItI